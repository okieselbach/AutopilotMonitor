// The session wire shape is GENERATED from the C# contract (SessionSummary in
// AutopilotMonitor.Shared → utils/wire-types.generated.ts) — field docs, optionality
// and the status union live there. This module only preserves the established
// `Session` name for the 25+ existing importers.
import type { SessionSummary } from "@/utils/wire-types.generated";

export type { SessionSummary };
export type Session = SessionSummary;
